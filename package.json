{
  "name": "dotmd-webcore",
  "private": true,
  "type": "module",
  "description": "Workspace root: the conversion engine and the live-page reader that feeds it",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun test",
    "lint": "bun run --filter '*' lint",
    "tsc": "bun run --filter '*' tsc"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/andryushkin/dotmd-webcore.git"
  },
  "homepage": "https://github.com/andryushkin/dotmd-webcore"
}
